# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Remove builds generated by Makefile
/builds

# Remove dist generated by Travis CI
/dist

# Include sdl2 windows dependencies
!/win-sdl2/**.*
